# 作品集霓虹 — Handoff Guide

项目别名：**作品集霓虹**（以后对 AI / 自己都用这个名字调用）。  
个人展示站（求职向）：`海外增长 · 用户增长 · 产品运营 · 内容运营 · 整合营销`。  
视觉与交互参考 `VA-11 Hall-A（赛博朋克酒保行动）`，并结合用户本人小红书哥特 Lolita 形象。

这份文档是给未来的你、别的电脑、或别的 AI / IDE 接手用的。重点不是解释每行代码，而是说明：

- 这个项目是什么
- 现在做到哪一步
- 平时主要改哪里
- 换电脑怎么继续
- 有哪些已知限制

## 当前状态

- 项目名：`作品集霓虹`
- 当前版本：`v3.1.0`（2026-07-17）
- 上一版：`v3.0`
- 类型：纯静态站点
- 技术：`HTML + CSS + JS`
- 运行方式：直接打开 `index.html`（现站）；新排版预览打开 `preview/index.html`
- 本地快照：`snapshots/作品集霓虹-v3.1.0.zip`（另有文件夹 `snapshots/作品集霓虹-v3.1.0`）

### v3.1.0 变更摘要

- 预览站对话流：进店欢迎台「点一杯 / 我先坐会」；酒单可选四杯特调
- 对话末尾：「结束对话」+「点一杯 / 再点一杯」（按是否已点过酒区分文案）
- 对话框内左侧纯文字「返回」；末句可结束或再点
- 特调短对话加长（约 3～4 层）；经历/作品弹窗与点击音效保留
- 中文像素字体 Cubic 11；舞台高度固定减少背景跳动

### v3.0 变更摘要

- 新增 **HUD 丰满排版预览站**（`preview/`）：左中主图无框铺满 + 右栏酒单/作品 + 底对话框
- 边框改为细线 / 虚线装饰 + **圆角**；导航、酒单、页脚等使用自绘 SVG 图标
- 背景使用模板场景抽出的纯酒吧立绘（`preview/assets/scene-bar.png`），UI 为代码叠层（非整页模板图）
- 导航下方功能键：NEXT / MUTE / LANG / CV；私人编辑台保留（`Ctrl+Shift+E`）
- 预览站中英切换补全（导航、面板标题、特调、播放状态、联系标签等随语言同步）
- 现站 `index.html` 仍为 v2.1.1 交互骨架，便于对比选型

### v2.1.1 变更摘要

- 新增**页面编辑模式**：`Ctrl+Shift+E` 输密码后，直接在网页上点文字改 / 点 × 删除
- 底部编辑条：保存（本机草稿）、导出草稿、高级面板、退出
- 编辑密码改为 `lorde0701`（以 `content.js` 为准，不被本机草稿覆盖）
- 修复密码遮罩 `hidden` 被 CSS 盖住、挡住原页面无法点击的问题

### v2.1 变更摘要

- 立绘改回**原图多帧切换**（不再用 SVG 人物）
- 眨眼：同构图闭眼帧整块替换眼区（含眉/鬓对齐），随机眨眼
- 说话：对话框打字时切换张嘴帧；眨眼+说话有 `both` 帧
- 去掉立绘校准 UI / 发丝裁切摆动层（避免盖回原睁眼）
- 编辑台仍可用：`Ctrl+Shift+E`，密码见 `content.js`

## 项目目标

做一个用于求职展示的个人网站，但不是传统简历模板站，而是：

- 保持 HR 可读性
- 保留 VA-11 Hall-A 式赛博酒吧氛围
- 有一定交互感
- 支持后续持续补充作品与图片

## 已确认需求

| 项 | 选择 |
|----|------|
| 语言 | 中英双语，默认中文 |
| 名字展示 | `刘骅慧 / Lorde` |
| 求职意向 | `海外增长 · 用户增长 · 产品运营 · 内容运营 · 整合营销` |
| 介绍语 | `把增长目标拆解成可执行路径的候选人` |
| 风格 | `VA-11 Hall-A` 赛博酒吧感 |
| 基础信息 | 不放年龄、性别、籍贯等 |
| 教育 | 只放最高学历：`香港岭南大学 · 文化研究硕士` |
| 经历 | 三段都放，保留关键成果 |
| 技能 | 精简标签云 |
| 作品 | `增长&营销案 / 内容与视频 / 摄影集` |
| 小红书 IP | 并入 `内容与视频` |
| CTA | 主：下载简历；次：加微信 |
| 联系方式 | 邮箱、手机、微信、小红书 |
| 适配 | 桌面 + 手机 |

## 已实现内容

### 页面结构

- 左侧：立绘视窗
- 右侧：酒吧菜单式内容切换
- 底部：对话框 / 打字机旁白
- 顶部：语言切换、静音、BGM 切歌

### 视觉与交互

- VA-11 风格整体色板与霓虹氛围
- 自定义小红书形象立绘
- 打字机旁白
- 菜单式区块切换
- 霓虹灯光变化
- 私人编辑台

### 音频

- 已内置 3 首本地可用 BGM：
  - `assets/audio/tracks/lorde-bar-1.wav`
  - `assets/audio/tracks/lorde-bar-2.wav`
  - `assets/audio/tracks/lorde-bar-3.wav`
- 右上角 `♪ BGM` 可切换
- `静音 / 声音` 可开关

### 动效

已接入（`v2.1` 起为同构图 PNG 帧切换）：

| 状态 | 文件 |
|------|------|
| 睁眼 idle | `assets/art/lorde-va11-bust.png` |
| 闭眼 blink | `assets/art/lorde-eye-closed.png` |
| 张嘴 speak | `assets/art/lorde-mouth-open.png` |
| 眨眼+张嘴 | `assets/art/lorde-blink-speak.png` |

- 眨眼：随机间隔，闭住约半秒再睁开
- 说话：旁白打字时嘴型开合
- 霓虹灯光变化

说明：更换主立绘后，需重新生成并对齐上述三张派生帧（或至少闭眼/张嘴两张）。

## 目录结构

```text
作品集霓虹/
├── index.html                # 页面骨架
├── css/
│   ├── styles.css            # 主视觉、布局、动效
│   └── editor.css            # 私人编辑台样式
├── js/
│   ├── content.js            # ★ 内容 / 配置 / 音乐 / 锚点主要改这里
│   ├── app.js                # 交互、音频、动效逻辑
│   └── editor.js             # 私人编辑台逻辑
├── assets/
│   ├── art/                  # 立绘资源
│   ├── audio/tracks/         # BGM 曲目
│   ├── cases/                # 营销案封面
│   ├── videos/               # 内容视频封面
│   ├── photos/               # 摄影集
│   └── resume.pdf            # 简历 PDF（需自行放入）
├── scripts/
│   └── gen-bgm.js            # 生成占位 BGM 的脚本
├── snapshots/                # 版本快照 zip / 文件夹
├── PROJECT.md
└── .gitignore
```

## 最常改的文件

### 1. 改文案

改 `js/content.js`：

- `name`
- `nameFull`
- `intent`
- `tagline`
- `about`
- `experience`
- `skills`
- `contact`

### 2. 改作品内容

改 `js/content.js`：

- `portfolio.cases`
- `portfolio.content`
- `portfolio.photos`

每一项通常会有：

- `cover`
- `title`
- `summary`
- `link`
- `tags`

### 3. 改音乐

改 `js/content.js` 的 `music.tracks`，并把音频文件放到：

`assets/audio/tracks/`

### 4. 改立绘

当前主立绘：

- `assets/art/lorde-va11-bust.png`

备份：

- `assets/art/lorde-gothic-bar.png`

替换立绘后，通常还要同步调整 `js/content.js` 中的：

- `portraitFx.eyeL`
- `portraitFx.eyeR`
- `portraitFx.mouth`
- `portraitFx.hairL`
- `portraitFx.hairR`

## 私人编辑台

### 打开方式

1. 打开页面
2. 按 `Ctrl+Shift+E`
3. 或地址栏加 `?edit=1`
4. 输入密码

密码写在：

- `js/content.js` → `editorPassword`

当前默认值：

- `lorde-edit`

建议后续自行改掉。

### 编辑台能做什么

- 改文字
- 改布局滑条
- 改作品封面、标题、链接
- 做立绘校准

### 立绘校准

进入编辑台后打开 `立绘校准` Tab：

- 绿框：眼睛
- 粉框：嘴巴
- 青框：发丝

用途：

- 对齐眨眼区域
- 对齐说话嘴型区域
- 对齐发丝摆动区域

调完后：

1. 点 `导出 content 草稿`
2. 把导出的内容合并回 `js/content.js`
3. 再上传到 GitHub

### 草稿机制说明

编辑台修改默认先保存在浏览器本机 `localStorage`。这意味着：

- 同一台电脑、同一浏览器能看到
- 换电脑不会自动同步
- 换软件也不会自动同步

如果发现“页面表现和文件内容不一致”，通常是本机草稿覆盖了文件内容。  
这时可在编辑台点：

- `清除本机草稿`

然后刷新页面。

## 本地预览

最简单方式：

1. 打开项目文件夹
2. 双击 `index.html`

如果浏览器缓存导致立绘或音乐不更新：

- 强制刷新页面
- 或清除本机草稿后再刷新

## BGM 说明

当前项目已经内置 3 首本地可用占位 BGM。  
如果想换成自己的歌：

1. 把音频放进 `assets/audio/tracks/`
2. 修改 `js/content.js` → `music.tracks`

支持思路：

- `.wav`
- `.mp3`
- `.ogg`

脚本：

- `node scripts/gen-bgm.js`

作用：

- 重新生成当前 3 首占位循环音轨

## 已知限制

### 1. 立绘动效是同构图多帧切换（非 Live2D）

`v2.1` 起：整张立绘帧互斥显示（`idle` / `blink` / `speak` / `both`），不是半透明遮罩叠眼。

优点：

- 融合稳，不易露出原睁眼
- 不依赖编辑台锚点

缺点：

- 换立绘后要重做派生帧
- 不是骨骼式连续动画
- 换立绘后通常要重新对准

### 2. 编辑台密码不是强安全

密码只用于防误触，不是后端鉴权。  
懂前端的人理论上仍可从源码看到。

### 3. 这台电脑没有本地 Git

当前这台电脑没有本地 Git，所以推荐先：

- 用 GitHub 网页版上传项目

等回到常用电脑后，再安装 Git 走标准流程。

## 跨电脑与跨软件

这个项目本质是静态文件，因此：

- Windows 可用
- macOS 可用
- Cursor 可接手
- VS Code 可接手
- 其他支持本地文件夹的 AI / IDE 也可接手

真正关键的是：

- 保留完整项目文件夹
- 保留 `PROJECT.md`
- 保留 `js/content.js`

## 后续优先事项

- [ ] 把当前版本上传到 GitHub，作为 `v2.0`
- [ ] 放入正式简历 `assets/resume.pdf`
- [ ] 补充营销案封面与外链
- [ ] 补充内容视频封面与外链
- [ ] 补充摄影集图片
- [ ] 继续微调眼睛 / 嘴巴 / 发丝动效锚点
- [ ] 如有需要，改用正式授权的 OST

## 站内公开联系方式

- Email: `lorde200071@163.com`
- Phone: `13738364702`
- WeChat: `UandME_blackandblue`
- 小红书: `https://xhslink.com/m/2imyOQhJjm4`
